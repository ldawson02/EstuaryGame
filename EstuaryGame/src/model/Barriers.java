package model;

public interface Barriers {
	/**
	 * author: rachel muzzelo
	 * 10/25/16
	 * Each barrier has build, decay, and crumble methods.
	 */

	void build();
	void decay();
	void crumble();

}
